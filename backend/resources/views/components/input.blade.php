<div class="col pb-3">
    <label class="pb-1" for="{{ $name }}">{{ $title }}</label>
    <input
        type="{{ $type }}"
        id="{{ $name }}"
        name="{{ $name }}"
        class="{{ $class }}"
        placeholder="{{ $placeholder }}"
        value="{{ $value }}">
</div>
